#include "ConnectionHandler.h"
int main(){
    ConnectionHandler h;
    h.Connect();

    // string str  = p.Next().Tostr();
    // std::cout<<str<<"\n";
    // Patient pat(str);
    // std::cout<<pat<<"\n";

}